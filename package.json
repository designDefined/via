{
  "name": "viajs",
  "private": true,
  "packageManager": "yarn@4.3.1",
  "workspaces": [
    "package/**/*",
    "demo"
  ],
  "scripts": {
    "demo": "yarn workspace demo",
    "via": "yarn workspace @viable/via",
    "via-react": "yarn workspace @viable/via-react",
    "build": "yarn core build && yarn react build && yarn demo build",
    "build:lib": "yarn core build && yarn react build"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5"
  }
}
